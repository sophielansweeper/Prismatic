---
id: pdf
title: PDF Component
sidebar_label: PDF
description: Search and extract data from PDF documents. Learn more about using Prismatic's PDF component.
---

[//]: # "DO NOT EDIT THIS FILE MANUALLY."
[//]: # "This file was auto generated using scripts in docusaurus/scripts/components/"

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

<div className="avatar">
  <div className="avatar__photo-link avatar__photo avatar__photo--lg">
    <img src="/docs/img/components/icons/60/cGRm.png" alt="PDF" />
  </div>
  <div className="avatar__intro">
    <h4 className="avatar__name">Search and extract data from PDF documents</h4>
    <small className="avatar__subtitle">
      Component key: <b>pdf</b>
    </small>
  </div>
</div>

## Description

The **pdf** component provides some actions for interacting with PDF documents, like finding text in a PDF document, listing page numbers, or extracting a specific page of a document.

This component cannot currently handle PDF documents that are encrypted.

## Actions

<div id="extractalltext" />

### Extract All Text

Extracts all the text from the specified PDF document and returns it as an array of texts. | <small><b>key: extractAllText</b></small>

<ComponentInputsTable
  inputs={[
    {
      placeholder: "PDF data",
      required: true,
      comments:
        "This must refer to a buffer containing the raw bytes of a PDF.",
      key: "pdfData",
      type: "data",
      default: "",
      label: "PDF data",
    },
  ]}
/>

#### Output Example Payload

```
{
  "data": [
    "This is an example text extracted from page 1 of a PDF document.",
    "This is an example text extracted from page 2 of a PDF document.",
    "This is an example text extracted from page 3 of a PDF document."
  ]
}
```

<hr className="componentActionSeparator" />

<div id="extractpage" />

### Extract Page

Returns the specified page in the given PDF document as a new separate PDF document. | <small><b>key: extractPage</b></small>

<ComponentInputsTable
  inputs={[
    {
      placeholder: "Page #",
      required: true,
      comments: "This specifies a page in a PDF document by number.",
      example: "5",
      key: "pageNumber",
      type: "string",
      default: "",
      label: "Page Number",
    },
    {
      placeholder: "PDF data",
      required: true,
      comments:
        "This must refer to a buffer containing the raw bytes of a PDF.",
      key: "pdfData",
      type: "data",
      default: "",
      label: "PDF data",
    },
  ]}
/>

#### Output Example Payload

```
{
  "data": {
    "type": "Buffer",
    "data": [
      69,
      120,
      97,
      109,
      112,
      108,
      101
    ]
  },
  "contentType": "application/pdf"
}
```

<hr className="componentActionSeparator" />

<div id="extractpagetext" />

### Extract Page Text

Locates and extracts pages text from the specified PDF document that matches the specified page range. | <small><b>key: extractPageText</b></small>

<ComponentInputsTable
  inputs={[
    {
      placeholder: "5",
      required: false,
      comments:
        "This specifies the ending page to extract from the PDF document. If not defined, will only extract the page on the pageStart input.",
      example: "5",
      key: "pageEnd",
      type: "string",
      default: "",
      label: "Page End",
    },
    {
      placeholder: "1",
      required: true,
      comments:
        "This specifies the starting page to extract from the PDF document.",
      example: "1",
      key: "pageStart",
      type: "string",
      default: "",
      label: "Page Start",
    },
    {
      placeholder: "PDF data",
      required: true,
      comments:
        "This must refer to a buffer containing the raw bytes of a PDF.",
      key: "pdfData",
      type: "data",
      default: "",
      label: "PDF data",
    },
  ]}
/>

#### Output Example Payload

```
{
  "data": [
    "This is an example text extracted from page 1 of a PDF document."
  ]
}
```

<hr className="componentActionSeparator" />

<div id="extracttextbypattern" />

### Extract Text by Pattern

Extracts text from the specified PDF document that matches the search text. | <small><b>key: extractTextByPattern</b></small>

<ComponentInputsTable
  inputs={[
    {
      placeholder: "true",
      required: true,
      comments:
        "This specifies whether searching should be case-sensitive. You can choose true or false.",
      example: "true",
      key: "caseSensitive",
      type: "boolean",
      default: "false",
      label: "Case Sensitive",
    },
    {
      placeholder: "10",
      required: false,
      comments:
        "This specifies the number of characters to extract from the PDF document after the search pattern found. If not specified, the entire page is returned.",
      example: "10",
      key: "charactersAfter",
      type: "string",
      default: "",
      label: "Characters After",
    },
    {
      placeholder: "pattern to search for",
      required: true,
      comments: "This is the text to search for in the PDF document.",
      example: "Some Text",
      key: "pattern",
      type: "string",
      default: "",
      label: "Search Pattern",
    },
    {
      placeholder: "PDF data",
      required: true,
      comments:
        "This must refer to a buffer containing the raw bytes of a PDF.",
      key: "pdfData",
      type: "data",
      default: "",
      label: "PDF data",
    },
  ]}
/>

#### Output Example Payload

```
{
  "data": [
    "This is an example text extracted from page 1 of a PDF document.",
    "This is an example text extracted from page 2 of a PDF document.",
    "This is an example text extracted from page 3 of a PDF document."
  ]
}
```

<hr className="componentActionSeparator" />

<div id="findpattern" />

### Find Pattern

Searches the PDF document and returns a list of page numbers containing text that satisfies the search criteria. | <small><b>key: findPattern</b></small>

<ComponentInputsTable
  inputs={[
    {
      placeholder: "true",
      required: true,
      comments:
        "This specifies whether searching should be case-sensitive. You can choose true or false.",
      example: "true",
      key: "caseSensitive",
      type: "boolean",
      default: "false",
      label: "Case Sensitive",
    },
    {
      placeholder: "true",
      required: true,
      comments:
        "This specifies whether to return page numbers that either contain or don't contain the search pattern. Options are true or false",
      key: "contains",
      type: "boolean",
      default: "true",
      label: "Contains?",
    },
    {
      placeholder: "pattern to search for",
      required: true,
      comments: "This is the pattern to search for in the PDF document.",
      example: "Some Text",
      key: "pattern",
      type: "string",
      default: "",
      label: "Search Pattern",
    },
    {
      placeholder: "PDF data",
      required: true,
      comments:
        "This must refer to a buffer containing the raw bytes of a PDF.",
      key: "pdfData",
      type: "data",
      default: "",
      label: "PDF data",
    },
    {
      placeholder: "false",
      required: true,
      comments:
        "This specifies whether the search pattern is a regular expression.",
      example: "true",
      key: "useRegex",
      type: "boolean",
      default: "false",
      label: "Use Regex",
    },
  ]}
/>

#### Output Example Payload

```
{
  "data": [
    1,
    2,
    3,
    4,
    5
  ]
}
```

<hr className="componentActionSeparator" />

<div id="pagenumbers" />

### Page Numbers

Returns a sequence of page numbers for the specified PDF document, from 1 to the last page number. | <small><b>key: pageNumbers</b></small>

<ComponentInputsTable
  inputs={[
    {
      placeholder: "PDF data",
      required: true,
      comments:
        "This must refer to a buffer containing the raw bytes of a PDF.",
      key: "pdfData",
      type: "data",
      default: "",
      label: "PDF data",
    },
  ]}
/>

#### Output Example Payload

```
{
  "data": [
    1,
    2,
    3,
    4,
    5
  ]
}
```

<hr className="componentActionSeparator" />
